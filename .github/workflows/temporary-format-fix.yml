name: Temporary Black format fix

on:
  push:
    branches: [fix/trade-ingestion-lossless]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.11"
      - name: Install Black
        run: python -m pip install black==25.12.0
      - name: Format affected files
        run: black aitos/data/ingestion.py tests/test_trade_ingestion_backpressure.py
      - name: Commit formatting changes
        run: |
          if git diff --quiet; then
            echo "No formatting changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add aitos/data/ingestion.py tests/test_trade_ingestion_backpressure.py
          git commit -m "style: format lossless trade ingestion changes with Black"
          git push
