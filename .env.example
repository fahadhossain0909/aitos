# AITOS Environment Template
# Copy to .env and replace ALL secret values before running production.
# NEVER commit .env or real credentials to Git.
#
# Conventions:
# - Normal variables below contain safe example values.
# - Secret variables are intentionally blank and include guidance on what to put there.
# - Generate secrets locally/VPS; do not copy example secrets into production.

# ============================================
# Application Environment
# ============================================
ENVIRONMENT=production
LOG_LEVEL=INFO
REQUIRE_HUMAN_APPROVAL_FOR_PROD=true
DEBUG=false
TESTING=false

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ============================================
# ClickHouse Configuration
# ============================================
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=aitos
CLICKHOUSE_DB=aitos

# ============================================
# Neo4j Configuration
# ============================================
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# ============================================
# Continual Learning
# ============================================
LEARNING_POLL_SECONDS=60
LEARNING_LOOKBACK_HOURS=168
LEARNING_MODEL_PATH=models/online_rl/deep_value.pkl
LEARNING_STATE_PATH=models/online_rl/worker_state.json

# ============================================
# Backtest / Historical Data
# ClickHouse is the preferred long-lived source. BACKTEST_DATA_DIR is only
# for optional local/Parquet replay and cache. It shares the unified boot-disk
# application-data budget; there is no separate backtest disk allocation.
# ============================================
BACKTEST_DATA_DIR=./.storage/others/backtest
OTHERS_DATA_DIR=./.storage/others

# ============================================
# Boot-disk safety policy
# The production CD enforces the same unified application-data budget.
# ============================================
OTHERS_MAX_GB=22.5
BOOT_FREE_BUFFER_GB=10

# ============================================
# Binance Trading Configuration
# Only needed for LIVE order execution.
# Keep TESTNET=true until live trading is deliberately enabled.
# Never grant withdrawal permission to a trading API key.
# ============================================
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_TESTNET=true
BINANCE_RECV_WINDOW_MS=5000
BINANCE_HEDGE_MODE=false

# ============================================
# Deployment Configuration (GitHub Actions)
# ============================================
DEPLOY_HOST=203.0.113.10
DEPLOY_USER=deploy
DEPLOY_PORT=22
APP_PORT=8000
APP_HOST=0.0.0.0

# ============================================
# Docker Compose / GHCR
# ============================================
COMPOSE_PROJECT_NAME=aitos
DOCKER_REGISTRY=ghcr.io
DOCKER_USERNAME=your_github_username

# ============================================
# Application Security
# ============================================
SECRET_KEY=
API_KEY=
DATABASE_URL=

# ============================================
# Optional Third-Party Services
# ============================================
SENTRY_DSN=
SLACK_WEBHOOK_URL=
